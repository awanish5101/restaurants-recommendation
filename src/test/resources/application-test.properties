# Overrides for the "test" profile (base config inherited from application.properties).
# Don't seed data or call the embedding API during context tests.
app.ingestion.enabled=false
app.embedding.enabled=false
gemini.api.key=
